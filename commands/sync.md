---
description: Sync current session to Obsidian markdown
user_invocable: true
---

Sync the current Claude Code session to Obsidian markdown.

Run:
```bash
uv run ${CLAUDE_PLUGIN_ROOT}/skills/session-export/scripts/claude-session-export.py sync --session-id ${CLAUDE_SESSION_ID}
```

Report the result to the user.
